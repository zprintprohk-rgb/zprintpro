# 挂账台账 · v9.2 任务 A 衍生（2026-09-10）

> 用途：下批（任务 B/C 或后续批次）报告「已知偏差/挂账清偿」段的闭环引用。
> 裁决来源：老板 2026-09-10 15:0x（会话指令，两项挂账裁定）。

## 挂账 1：en/ja 纸袋导航位次 × 下拉偏移共用值

**状态**：✅ 已裁定 + 前置验证完成 → **走 C（同步位次）**，进下批执行，不直推。

**前置验证结论（en/ja 排序 = 非有意）**，证据：
1. en navOrder（Header.tsx L63）与 ja navOrder（L86）行谱系 = 仅 `d3f0994f`（V21 复制创建）一个提交，**从未被修改**——不存在刻意的 en/ja 重排；
2. zh-hk navOrder 由 `db2cb5f2`（K3 8/11 10:33，按 GSC 搜索量）刻意重排：纸袋 3 imp 垫底 → 第 1 位移至最后；en/ja 未同步 = 残留 V21 默认序；
3. pillar v2（`01458676`，9/1）纸袋降级 L3，「导航从主营区移到更多產品」为 §11.6 允许动作——en/ja 首位与战略矛盾；
4. 生产 en 首页实测 paper-bags 链接仍在导航首位（HTML 位置实证）。

**下批执行项（C）**：en/ja navOrder 纸袋移至末位（与 zh-hk 对齐）→ 三语言共用 `dropdownOffsets['paper-bags'] = -240px` 均正确。若执行中发现 en/ja 存在未预见的布局依赖（如 flex-1 均分宽度变化），退 B（per-locale 拆分：zh-hk -240 / en/ja +160），并在清偿段注明退路原因。

## 挂账 2：check-bc-ban.mjs 对 redirect 源行误报

**状态**：✅ 已裁定 = 放行维持（继续按 §0.0.1 豁免 + §6.1 声明放行处理），**追加授权下批修脚本白名单**。

**下批执行项**：修 `scripts/check-bc-ban.mjs`，豁免面**严格限定** = `next.config.js` 中 `GSC_404_R2` 数组内的 redirect 源行（当前命中：第 3 条 `/zh-hk/zh-hk/product/eco-business-cards → /zh-hk/category/packaging/`）。禁止扩大豁免到任何其他位置/形态；修后重扫验证：GSC_404_R2 源行 0 报 + 非 redirect 的名片词命中仍 0。

## 清偿闭环要求（下批报告必含段）
「已知偏差/挂账清偿」段落逐条列：挂账 1（C 执行结果 / 或退 B 原因）、挂账 2（脚本白名单落地 + 重扫结果），引用本台账。

## 数据来源
- 老板裁决：会话指令 2026-09-10（两项裁定）
- git 实证：db2cb5f2（8/11 zh-hk 重排）/ d3f0994f（V21 创建）/ 01458676（pillar v2）/ L63/L86 行谱系
- 生产探针：zprintpro.com/en/ 导航序 2026-09-10 15:0x HKT
