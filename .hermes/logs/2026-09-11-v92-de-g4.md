# v9.2.3 D+E+G4 合批执行报告 (2026-09-11)

> 老板 9/11 指令「按 v9.2.3 顺序: → D+E+G4 合批」。
> **1 commit**：`8738a047` feat(pages): D Blog 列表重设计 + E Contact 重设计 + G4 (contactPoint/AEO) → main merge `0507327b`。

## 一、任务 D — Blog 列表页 (/blog/) UX/UI 重设计

按指令 6 块结构，视觉语言与 PLP v9.1 同族（三色调令牌 / 1320px / 满版色交替）：
1. **Banner**：藏青渐变（`--color-royal-navy-grad`）+ H1 印刷知識 + 副标 + 右侧几何装饰（同心圆环/圆点网格，纯 CSS 无图）
2. **精选/最新区**（`#F2F6FF` 浅蓝带）：头条大图卡 1 张 `aspect-[2/1]`（lg 占 2 列）+ 次条 2 卡 `aspect-[16/9]` 右列纵排
3. **分类筛选条**：单行紧凑胶囊（SpecFinder 同语言），overflow-x-auto 无滚动条样式，含各分类计数，激活=橙
4. **文章网格**：白底 3 列（sm:2 / lg:3）白卡（封面 16:9 + 分类胶囊 + 标题 line-clamp-2 + 摘要 line-clamp-2 + 日期·阅读时长等宽小字）；**hover 仅边框+阴影，无 translate/scale**
5. **满版色交替**：精选区浅蓝 / 网格白 / 页底 CTA 藏青渐变
6. **页底 CTA**：藏青渐变圆角 + 橙「免費報價」（→ /quote/）+ 绿 WhatsApp
- **真实阅读时长**：`blog/page.tsx` 服务器端从 blog-data JSON 真实正文计算（zh 400 字/分、en/ja Intl.Segmenter 200 词/分），非编造
- 保留：hero WhatsApp 008 埋点（`whatsapp_click/blog-hero`）+ 页底 2 埋点（`contact_click`/`whatsapp_click` `blog-bottom-cta`）
- **禁动清单验证**：slug/标题/摘要/blog 详情/articleSlugs 零改动（blog-posts.ts 只读；详情页只探针回归）

## 二、任务 E — Contact 页 (/contact/) UX/UI 重设计

1. **Banner**：藏青渐变 + H1 + 副标（指令原文「2 小時內回覆 · WhatsApp 即時報價」/ en / ja）+ 几何装饰 + SLA 条款链接（`parseInlineLinks` 正确渲染，修掉原 `<p>{t.subtitle}</p>` 未解析 markdown 的问题）
2. **信任带 4 徽章**（既有文案）：免費打樣·滿意再下單 / 即日交貨·特急24小時 / ISO 9001 認證·品質管理 / 順豐直達·全港覆蓋（en=Free Sample/Same-Day Delivery/ISO-Certified Quality/DHL Express Direct；ja=無料サンプル/即日納品/ISO認証品質管理/国際配送）
3. **双栏**：左=QuoteForm（`/api/quote` 字段映射**零改动**）；右=联系卡（WhatsApp 大按钮 / 電話 / 電郵 / NAP 深圳地址一字不改 / 服務時間 / QR）
4. **FAQ 手风琴**（`<details>` 纯 HTML，无客户端 JS）：现有 SLA FAQ 前 4 条（回覆 2 + 打樣 2），三语言
5. **页底满版 CTA**：藏青渐变 + 橙免費報價 + 绿 WhatsApp
- **008 埋点保留**：`contact_phone_click` / `contact_email_click` / `contact_whatsapp_247_view` / `contact_support_view` 全数保留 + 新增 WhatsApp/CTA `data-event`（contact-hero / contact-whatsapp-card / contact-bottom-cta）
- JsonLd 保持 1 个数组形式（防 streaming 500）

## 三、任务 G4 — Contact/即日页等服务页（随 D/E）

1. **Schema**：ContactPage（既有）+ `generateBusinessJsonLd` contactPoint **补 email + WhatsApp url**（真实 NAP，§13.10 法务层）
2. **GEO**：NAP 真实信息一字不改（深圳地址 +86 198 8085 1334 / zprintpro@outlook.com），Schema address 与 Footer 一致
3. **AEO**：即日页 Hero 下 40-60 字答案块（`page.tsx` 非冻结区，Rush* 组件未动）：「即日印刷：每日 18:00 前落單並確認稿件，即安排通宵印刷，順豐翌日中午 12:00 前送到，100 張起印。」（§0.23 唯一口径，取本页 FAQ verbatim，en/ja 同步）

## 四、验收（本地门禁全绿）

| 门禁 | 结果 |
|---|---|
| tsc --noEmit | 54=54 基线（quote-engine __tests__ 既有），变更文件 0 错 |
| npm run build | exit 0 |
| bc-ban diff | 0 命中（types/quotation.ts + next.config.js 注释 = 基线豁免） |
| encoding | UTF-8 LF 通过（5 文件） |

## 五、已知偏差（待老板复核）
1. **E hero 副标「2 小時內回覆」= 指令原文**，与站内既有口径（24h SLA FAQ / 15 分鐘專人回覆 / 5 分鐘回覆 FloatingQuoteCTA）并存——同页出现多口径数字，**建议老板拍板统一**（或改回 24h 链接口径）
2. **Blog 阅读时长**为算法估计（zh 400 字/分、en/ja 200 词/分），非官方统计，展示为「N 分鐘/読了 N 分/min read」
3. **trust 带 ISO 9001 徽章** = 既有站内语言（HowTo/schema/ProductWhyChooseUs 描述），非新增认证声明
4. **Blog 列表移除左栏**（內容分類/熱門產品）→ 按指令改为单行筛选条；熱門產品区块无 008 埋点，无跟踪损失
5. FAQ 手风琴仅取 SLA FAQ 前 4 条，非全量 FAQ（/faq/ 页不受影响）

## 六、数据来源
- D 结构/文案：v9.2.3 指令 D 节 + blog-posts.ts（只读 title/excerpt/cover/date/categoryKey）+ src/data/blog-data/{zh-hk,en,ja}.json（阅读时长真实正文）
- E 结构/文案：v9.2.3 指令 E 节 + 既有 contact translations + ProductWhyChooseUs.tsx（信任徽章）+ src/data/faq/{locale}.json（SLA FAQ 前 4 条）+ siteConfig NAP（真实）
- G4：v9.2.3 指令 G4 节 + generateBusinessJsonLd（siteConfig NAP）+ rush page FAQ verbatim（18:00 截單→翌日中午口径）
- 探针：CF Pages deployments API（deployment_trigger.metadata.commit_hash）+ zprintpro.com 线上 HTML

## 七、线上验收（deploy 后）
- commit 8738a047（+C3 回填 900e73e7 同批）→ main merge `0507327b` → CF deploy `89746237` build=success/deploy=success（2026-09-11 06:0x）
- **D/E/G4 目标探针 42/42 全过**：D blog 3 语言（H1/精選/筛选/底 CTA/阅读时长）+ 详情 FAQPage 3；E contact 3 语言（副标/信任徽章/FAQ/WhatsApp/埋点）+ schema（email/ContactPage）；G4 rush AEO 3 语言
- 回归 6 针：4 针首轮猜词错误（首页/PDP CTA 文案与针不同，页面均 200 正常加载）→ 用真实文案复探 4/4 过（zh 線上30秒獲取初步報價 / en 30-second AI quote / ja 見積もり / en PDP Instant Quote）
- **线上 48/48 有效**（44 首过 + 4 针修正）
