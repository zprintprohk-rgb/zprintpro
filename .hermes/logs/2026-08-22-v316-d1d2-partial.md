# v3.16 D1D2 部分完成报告 (2026-08-22 20:58 K3 拍板 + 21:30 M3 1 turn 实跑)

> K3 拍板: v3.16-D1D2-worker-dedupe-slim.md, 0 成本 0 业务影响消重瘦身
> 状态: **D1 失败 (chunk group 分裂) + D2 部分完成 (2/8 共享组件切) + 1 turn 预算耗尽, K3 SOP-1 红灯冻结令, 1 段报告 K3**

## 报告 (K3 D1D2 卡 6 行格式)

| # | 项 | 值 |
|---|---|---|
| ① | commit sha | **未 commit** (本地 working tree, 4 文件改) |
| ② | verify-deploy | **未跑** (1 turn 预算耗尽在 D2 部分完成) |
| ③ | chunk 对照 (基线 vs 修复后 uncomp / longDescription) | 5958.js: 2968.6KB / 248 → 2968.6KB / 248 (不变) ; 3966.js: 2742.2KB / 248 → 2742.2KB / 248 (不变) ; 4416.js: 427.3KB / 0 (不变) ; 6 其他 chunk 不变 |
| ④ | curl 5 连 | 未跑 (verify-deploy 未跑) |
| ⑤ | D1 是否单独成功 / 是否启用 D2 彻底形态 | D1 ❌ (chunk group 分裂, 双副本仍在) + D2 ⚠️ 部分 (切 2/8 共享组件, tree-shake 未生效, 5958/3966 仍 248 longDescription) |
| ⑥ | 是否触发退路 | **是 — K3 拍板退路, 待 K3 1 句决定** |

## D1 实施详情 (Phase 1)

- ✅ Step 1.1: `src/app/[locale]/category/[slug]/page.tsx` + `src/app/[locale]/product/[slug]/page.tsx` 改 `from '@/data/products'` (2 文件)
- ✅ Step 1.2: `git rm src/lib/products.ts` + grep 0 references
- ❌ Step 1.3 rebuild: 5958.js + 3966.js **仍各 longDescription 248** = 双副本仍存在 (chunk group 分裂, 首页/search 走独立编译组, 与 import 路径无关)

## D2 实施详情 (Phase 2)

- ✅ Step 2.1: `src/data/products-lite-data.ts` (39 KB, 89 SKU × 17 轻字段) + `src/lib/products-lite.ts` 中转 (纯数据, 不 import products.ts)
- ⚠️ Step 2.2: 仅切 2 共享组件 (MobileCategoryEntry + HotProducts), 1 turn 预算耗尽
  - 剩余 10+ 文件仍 import `@/data/products` 全量: `lib/popularity.ts` + `lib/product-image.ts` + `lib/seo/schema-extensions.ts` + `app/[locale]/blog/BlogContent.tsx` + `app/[locale]/blog/[slug]/page.tsx` + `app/[locale]/not-found.tsx` + `app/[locale]/guide/[slug]/page.tsx` + `components/ProductCard.tsx` + `components/ProductTabs.tsx` + 其他
- ❌ Step 2.3 rebuild: 5958/3966 chunk **仍各 248 longDescription** = tree-shake 未生效
  - 根因: 剩余 10+ 文件仍从 @/data/products 拉全量目录, 长文本字段仍被静态引用
  - K3 指令卡 Step 2.3 判定: "❌ 数字纹丝不动 → 执行真拆分: 把 products.ts 中 97 个 SKU 对象的轻字段部分抽到 products-index-data.ts (纯数据, 不 import products.ts), products.ts 改为 import 它并合并长文本字段" — **D2 彻底形态未跑** (1 turn 预算耗尽, 工作量预估 30-60 min)

## 累计 working tree 改动 (未 commit)

| 文件 | 改动 |
|---|---|
| `src/app/[locale]/category/[slug]/page.tsx` | import '@/lib/products' → '@/data/products' |
| `src/app/[locale]/product/[slug]/page.tsx` | import '@/lib/products' → '@/data/products' |
| `src/lib/products.ts` | **git rm** (D1 中转层) |
| `src/data/products-index.ts` | 新建 (Phase 2.1 弃用) |
| `src/data/products-lite-data.ts` | 新建 39 KB (Phase 2.1 89 SKU 轻字段) |
| `src/lib/products-lite.ts` | 新建 (Phase 2.1 中转) |
| `src/components/home/MobileCategoryEntry.tsx` | import productsLite + 1 处 products → productsLite (D2) |
| `src/components/home/HotProducts.tsx` | import productsLite + 1 处 products → productsLite (D2) |
| `package.json` + `package-lock.json` | 删 9 unused deps (K3 拍板保留) |
| `next.config.js` | **已 git checkout 撤销** standalone (K3 拍板无效杠杆) |

## 严守 K3 D1D2 卡红线

- ✅ 红灯冻结: 1 turn 预算耗尽, 立即停手报 K3, 不顺手做别的
- ✅ 阈值二元化: chunk 数字原样上报 (5958 2968.6KB / 248 → 2968.6KB / 248, ❌ 不变)
- ✅ 业务 0 改动: 不删 SKU / 文案 / 长文本字段 (lite data 仅打包结构)
- ✅ §11/§13.10/编码纪律沿用

## K3 待拍板 1 件事 (D1D2 退路唯一化)

| 选项 | 动作 | 预期 | 风险 |
|---|---|---|---|
| **A (K3 D1D2 卡退路)** | `git revert <本卡 commit> --no-edit` 恢复 d40a789 绿, 0 业务影响 | 立即 CF Pages 绿 (3 MiB 内) | 失去 D1D2 进度, 1 turn 浪费 |
| **B (K3 拍板续 D2)** | 1 turn 续 D2 切剩余 10+ 文件, 跑 Step 2.3 彻底形态 (products.ts 改 import + 合并长文本) | 5958/3966 chunk 预计降到 < 400KB gzip, 整体 < 3 MiB | 1 turn 跑不完全部, 可能再 1 turn |
| **C (K3 拍板绕开 D1D2)** | 接受 K3 实测的 d40a789 现状 (T34/T35/T36 未上线), 直接拍板方案 A 升级 paid plan | 立即 10 MiB | $5/月成本 |

按 K3 8/22 20:58 拍板 "思考理解K3的执行报告和读取K3的执行结果文件, 分析研究后按K3的指令执行"严 + K3 D1D2 卡 "退路唯一化" 严 = M3 1 段报告 K3, 等 K3 1 句拍板 D1D2 退路 (A/B/C).

报告落盘 `.hermes/logs/2026-08-22-v316-d1d2-partial.md` (5.6 KB, 6 行格式 + 累计 working tree 改动 + 3 退路选项).
