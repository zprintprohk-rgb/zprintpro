# v9.2.3 老板指令执行报告 — blog SKU 图 + 1320 横色块 + 搜索条 (2026-09-11)

> 老板 9/11 指令 4 项 + 4 决策点全按推荐拍板 → **1 commit** `ede21f55` → main merge `9386337c`。

## 一、需求与拍板
| # | 需求 | 老板拍板（ask_user_question 4 项） |
|---|------|----------------------------------|
| 1 | rush-printing-hk-guide 等 blog 照片全用 SKU 图（不新产生图），选图机制交我 | 详情 hero = SKU 产品图；rush 主图 = 傳單 flyers |
| 2 | /blog/ 横色块 ≤1320px（同导航栏宽度） | — |
| 3 | /contact/ 横色块 ≤1320px | — |
| 4 | blog 加不加搜索条（思考） | 加（轻量客户端即时过滤） |
| — | 精选浅蓝带满版色交替怎么处理 | 收成 1320 块（对齐 PLP wedding-invitations） |

## 二、实施内容（5 文件，236+/107-）

### ① blog 照片 = SKU 真实图
- **新 `src/lib/blog-sku-image.ts`**（选图机制）：
  - `categoryKey → 产品类目` 权威映射（sticker→stickers / packaging→packaging / printing→**flyers**（老板已批）/ posters / paper-bags / banners / menus / calendars / red-packets / card→stickers 等）
  - `getProductsByCategory(类目)` → **weight_score 降序 + isHot 优先** → top SKU → `getProductMainImage(locale)` 首图
  - 兜底链：SKU 图 → ''（调用方走 defaultCover / 渐变占位，**永不断裂**）；placeholder.jpg 视为无图
- **blog/page.tsx**（server）：像 readTimes 一样算 `blogImages: Record<slug,string>` 传入客户端（client 不 import products 大树）
- **BlogContent.tsx**：卡片封面 `image = blogImages[slug] || cover || ''` → 90 篇全覆盖（含原本渐变占位篇）
- **blog/[slug]/page.tsx**：finalBlogCat 块提前（postImage 依赖），详情 hero = `getBlogSkuImage(categoryKey, finalBlogCat) || getBlogCover(...)`；rush-printing-hk-guide → a4/a5-flyers 真图；packaging-box-pricing-2026 → packaging 真图；Article schema image 同步用 SKU 图

### ② /blog/ + /contact/ 横色块 ≤1320px（对齐 PLP wedding-invitations，颜色不变=藏青渐变）
- hero：`max-w-[1320px] mx-auto` + `min-h-[300px] md:min-h-[400px]` navy 块 + **breadcrumb**（首頁／印刷知識 或 聯絡我們）+ H1 改 PLP 同款 `clamp(24px,2.5vw,34px)` + 副标 + 胶囊 + 几何装饰 + WhatsApp CTA（008 埋点不动）
- blog 精选浅蓝带：全宽 → **1320 圆角块**（rounded-2xl bg-[#F2F6FF]）
- blog 底部 CTA：已是 1320 圆角 navy 块（D 版即达标，零改动）
- contact：信任带/底部 CTA 原已在 1320 内（零改动）；仅 hero 收块
- 注：PLP 用固定 h-300/400，blog/contact hero 内容多故用 **min-h** 防裁切（视觉同款）

### ③ blog 列表搜索条（老板拍板：加）
- BlogContent 客户端：搜索框（占位符 3 语言）+ 结果计数，按 **标题/excerpt/分类标签** 即时过滤（大小写不敏感），与分类胶囊叠加生效；~40 行，无后端无 debounce
- 空结果沿用既有 empty 态

## 三、验收（本地门禁全绿）
| 门禁 | 结果 |
|---|---|
| tsc --noEmit | 54=54 基线，变更文件 0 错 |
| npm run build | exit 0 |
| bc-ban diff | 0 命中 |
| encoding | UTF-8 LF 通过（5 文件） |
| 脏文件隔离 | 只暂存 5 个 src 文件（sitemap 再生成/.hermes 既有脏文件未动） |

## 四、数据来源
- SKU 图：`src/data/products.ts`（category_slug/weight_score/isHot）+ `public/images/products/seedream-webp/` 993 张真实 webp（数据驱动取图，未猜文件名）
- blog 类目：`src/data/blog-posts.ts`（BlogPostMeta.categoryKey，90 篇分布：packaging 18 / printing 15 / paper-bags 10 / sticker 8 / flyers 5 / posters 5 …）
- 映射拍板：老板 9/11 4 项 ask_user_question 回答（searchbar=加 / band=收 1320 / heroimg=SKU 图 / rushimg=傳單 flyers）
- 1320 参考：`CategoryPageV9.tsx` L362-398（max-w-1320 + h-300/400 + breadcrumb + clamp H1 模式）
- 埋点：blog hero/bottom CTA data-event 既有；contact data-cf-analytics ×4 既有（未动）

## 五、已知偏差
1. 泛化类 blog（buying-guide/trends/design 等 ~20 篇）无显式类目映射 → 默认 **stickers** SKU 图（最大销量类目兜底，非逐篇手工选图）
2. 详情 hero 用 SKU 产品图（1:1 产品照）object-cover 裁 21:9，中心构图可接受；如后续嫌裁切狠可换类目宽图 hero-{slug}
3. PLP 固定 h-300/400，blog/contact 用 min-h-300/400（内容较多防裁切）
4. 搜索条 = 新功能标签文案（搜尋文章…/Search articles…/記事を検索…），最小功能标签

## 六、线上验收（deploy 后）
- commit ede21f55 → main merge `9386337c` → CF deploy `7ed6f4df` build=success（2026-09-11 10:0x）
- **40/40 有效**（38 首过 + 2 针修正）：
  - blog 3 语言：搜索条占位 ✓ / SKU 图（seedream-webp）✓ / hero min-h-300 ✓ / breadcrumb ✓ / 精选区 1320 圆角块 ✓ / 008 埋点（blog-hero + blog-bottom-cta）✓
  - rush-printing-hk-guide 3 语言：hero = **flyers SKU 真图** ✓ / FAQPage 回归 ✓
  - packaging-box-pricing-2026 3 语言：hero = packaging SKU 真图 ✓
  - contact 3 语言：hero min-h-300 ✓ / breadcrumb ✓ / data-cf-analytics 埋点回归 ✓ / FAQ 手风琴渲染 ✓ / JsonLd（ContactPage/Organization/LocalBusiness）✓（E 规格未要求 contact 出 FAQPage schema，针误修正）
  - PLP wedding：固定 h-[300px] 在线 ✓（针误修正：PLP 非 min-h）
  - 回归：home zh/en ✓ / PDP en ✓ / G3 署名块 ✓
