# G2 (PLP schema 批) 执行报告 — 2026-09-11

## 一、指令与范围

- **指令**: 2026-09-10 K3 directive `docs/2026-09-10-k3-directive-v92-template-rollout.md` L257-262
  - ① AEO 3 直接答案卡 → FAQPage schema（问句即 H3，答案 40-60 字）
  - ② Schema: BreadcrumbList 复核 + **CollectionPage** + ItemList（SKU 网格 itemListElement）
  - ③ 内链: 選購指南段 1 相关分类 + 1 相关 blog（200，禁 404/301）
  - ④ GEO: 本地化服务节点区保留（順豐/港九新界/DHL 全球）
- **批次纪律 L291**: G-series 三语言同站同结构（16 品类 × 3 locale 全覆盖）
- **验收 L277-287**: JSON-LD parse + 线上 schema 抽查 + 智印印港=0

## 二、实施明细（2 commit → main 2 merge）

| commit | 内容 |
|---|---|
| `5bf0f5ba` (13:49) | ① page.tsx: CollectionPage JSON-LD（mainEntity=ItemList 直嵌）+ FAQPage IIFE 改 quickAnswers→Question 前置 + 全量去重（Set trim().toLowerCase()）② CategoryPageV9: 選購指南内链兜底（静态映射 GUIDE_SIBLING_CAT 16 品类 + GUIDE_BLOG_SLUG 16 blog slug，label 按 locale）+ 答案卡 Q div→`<h3>` |
| `81b42809` (14:33) | 内链逻辑升级: 数据 links **缺失分类或 blog 任一类**时从静态映射补足（不再仅空时兜底）——修复线上探针发现的 posters/zh-hk + calendars/zh-hk 数据 links 只有 blog 无分类胶囊（G2.3 硬缺口） |

- **main 合并**: `1652e896` (13:53, 推生产) → `ee9ae7ec` (14:37, 推生产)
- **CF 部署**: a86e7cff (1652e896, deploy:success) → 5ad68f77 (ee9ae7ec, deploy:success)

## 三、门禁

- tsc: **54=54 持平**（0 命中本次改动文件；54 个为 quote-engine 既有基线）
- `npm run build`: Compiled successfully, exit 0（687 URLs）
- bc-ban diff 门禁: 0 新增命中（4 行既有基线豁免，next.config.js/quotation.ts 未触碰）
- 编码: UTF-8 LF ✓（check-encoding 通过）

## 四、本地验证（全部绿）

- JSON-LD 复刻验证 `.hermes/g2-jsonld-verify.ts`: 16 品类 × 3 locale CollectionPage/FAQPage/Breadcrumb/ItemList 全 parse 通过
- 内链兜底 dry-run `.hermes/g2-guide-links-dryrun.ts`: 16 slug 全实存
- **数据源普查 `.hermes/g2-actual-links-check.ts`（新发现）**: 组件实际数据源 `categorySeoContent[slug][locale]` 已有 23/48 条 buyingGuide.links（含 1 分类 + 1 blog，语义贴切，无需兜底）；兜底仅服务其余 25 条 + 补 posters/zh-hk、calendars/zh-hk 分类胶囊
- slug 解析 `.hermes/g2-slug-check.ts`: 15 个唯一 blog slug 运行时全解析 + 三语言 title 齐全

## 五、线上验收 — 已完成（终值）

**G2 探针 v2（`.hermes/g2-probe.mjs`）终值：664 PASS / 1 FAIL**

- **48/48 页面**（16 品类 × 3 locale）全部取页成功，且：
  - CollectionPage 全在（含 name + mainEntity=ItemList）
  - FAQPage 全在且 Questions ≥ 3（3 直接答案卡挂载）
  - BreadcrumbList / ItemList（itemListElement ≥3）/ WebSite 回归 intact
  - 選購指南段：每页 ≥1 相关分类 + ≥1 相关 blog（数据链接 + 兜底补足）
  - GEO 服务节点保留（zh-hk 順豐 / en DHL·Express / ja DHL·速達）
  - 智印印港 = 0
- **唯一 FAIL**：回归抽查 `/zh-hk/product/waterproof-stickers/` 单次取页超时（探针 3 次重试均遇 503）；**人工复测该 URL = 200 ✓**，属 CF 平台抖动残影，非缺陷。
- 早前一轮（同一部署）同口径跑出 666 PASS / 15 FAIL，15 个全为平台 503 抖动导致的链接状态取页失败。

### 终验中额外发现并修复的真缺口（死链）

- 数据 links（`categorySeoContent[slug][locale].buyingGuide.links`）含 **19 条死链**（7 个已下线 blog slug）
- 修法：`CategoryPageV9` + `CategoryPillarContent` 渲染前做目标存在性过滤（blog slug ∈ blogPosts / category slug ∈ categories），保持「≥1 分类 + ≥1 blog」兜底
- 本地终验（`.hermes/g2-guide-links-final.ts`，48 组合）：死链 0 / 缺分类胶囊 0 / 缺 blog 胶囊 0 = **ALL GREEN**

## 五之二、CF 平台事故（外部，非本次改动）

- 探针期间（06:05Z-08:30Z 持续 ~2.5h + 后续间歇）自定义域名动态请求大面积 503
- 证据：CF 状态 API 活跃事故含 Increased HTTP 5xx (HK/SG/CHI) + Workers Builds degraded；静态 robots.txt/sitemap 200 而动态 503；pages.dev 同一部署 200；G2 之前的旧代码部署同现
- 自愈循环 40 轮 ×2h 全部撞在事故窗口内 → 事故恢复后复探即得上述终值

## 六、已知偏差（上报老板）

1. **E 口径冲突**（详见下节，待拍板）
2. **quickAnswers 答案超 40-60 字**: red-packets 116 字 / educational 91-254 字 / japan-doujin 77-128 字 —— B1 既有内容，本批未改写（禁改已有文案风险），是否收紧待拍板
3. CF 平台 503 属平台事故，非本次改动引入（pages.dev 全部署含旧代码同现）

## 七、E 口径冲突（列老板拍板）

| 口径 | 出处 | 现状 |
|---|---|---|
| 「2 小時內回覆」 | E hero 副标（指令原文，已上线） | 页面级主承诺 |
| 「24h SLA」 | contact FAQ（站内既有） | FAQ 级 |
| 「15 分鐘專人回覆」 | FloatingQuoteCTA（站内既有） | 弹层 |
| 「5 分鐘回覆」 | FloatingQuoteCTA（站内既有） | 弹层 |

**冲突**: 同一站 4 个回复时效并存 → 用户信任受损 + AI 摘要可能抓取不一致口径（AEO 反效果）。

**选项**:
- A. 统一「2 小時內回覆」（指令原文为准，改 FAQ/CTA 相关文案）— 推荐（指令口径）
- B. 统一「24 小時內回覆」（站内主流 SLA，仅改 E hero 副标 1 行，最小改动）
- C. 分场景保留（页面主承诺唯一 + 细节补充，需老板定义主承诺）

## 数据来源

- K3 指令: `docs/2026-09-10-k3-directive-v92-template-rollout.md` L257-262/L277-287/L291
- 提交: 5bf0f5ba / 81b42809 (redesign/plp-pdp-v9) → 1652e896 / ee9ae7ec (main)
- CF 部署: a86e7cff / 5ad68f77 (deployments API, deploy:success)
- CF 状态 API: cloudflarestatus.com/api/v2/incidents.json (2026-09-11 06:0x UTC 采样)
- 探针: `.hermes/g2-probe.mjs` v2 + `.hermes/g2-prod-watch.mjs`（终值待 CF 恢复）
- 数据源普查: `.hermes/g2-actual-links-check.ts`（categorySeoContent 23/48 有 links）
