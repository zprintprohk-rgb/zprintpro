# M3 执行卡 v3.17-B5 · T39 IndexNow 自动化入 cron (K3 8/24 11:37 拍板)

> 签发: M3 (Mavis) · 2026-08-24 12:05
> 拍板: K3 v3.17 战略 8/24 11:37 "5 批串行" B5 排期 (17:30-18:15, B4 verify PASS 后立即派工, 工具层殿后承接 B1-B4 全部新 URL)
> 性质: **B5 串行 P0**, 工具层, 承接 B1-B4 全部新 URL 自动化提交
> 战略依据: `docs/k3-strategy-v3.17-seven-tasks-today-2026-08-24.md` §一 B5 + v3.16 §七 T39
> 目标: IndexNow 自动化脚本 + daily cron prompt 集成 + 首批 B1-B4 新 URL 提交, 1 src commit 1 push + 1 cron update

---

## 〇、前置状态确认 (B4 verify PASS 后立即执行)

1. **B1 + B2 + B3 + B4 verify-deploy PASS** (12:00-17:30): T45 + T42 + T44 + T41 + G1 Vol.2 src/ 改动已上线, 39+ routes + G1 schema 增量
2. **IndexNow 现状** (K3 v3.16 §七 T39 PENDING 8/28 → v3.17 8/24 今日): 0 自动化, 0 提交
3. **B4 手动提交 G1 URL 已完成** (B4 末步骤), 但其他 B1-B3 新 URL 未提交
4. **业务 0 改动红线** (K3 8/22 17:58 F0): 不删任何 SKU/文案/长文本字段
5. **IndexNow key 需 K3 提供** (per 008 度量层 8/29 校准, K3 拍板提前到 8/24)

---

## 一、B5 范围 (1 脚本 + 1 cron prompt 集成, 1 src commit + 1 cron update)

| # | 动作 | 涉及文件 | 内容 |
|---|------|---------|------|
| 1 | **IndexNow 自动化脚本** | scripts/indexnow-auto-submit.py (新建) | 每日扫 sitemap.xml + 新 URL diff + POST IndexNow API |
| 2 | **daily cron prompt 集成** | mavis cron update 4 条 zprintpro cron (T39 段) | 必含 v3 §A 17 B5 段, daily-cron 8/25+ 启动 T39 |
| 3 | **首批 B1-B4 新 URL 提交** | manual 1 次 (push 后) | 提交 B1-B4 全部新 URL (T45 envelopes + T42 7 类目 + T44 ja 5 类目 + T41 catalog PDP + books + blog 新路由 + G1 Vol.2 3 locale) |
| 4 | **README 文档** | scripts/README-indexnow.md (新建) | 脚本使用 + K3 key 注入 + 监控 + 退路 |

**总改动**: 1 脚本 + 1 README + 4 cron prompt update (T39 段) + 1 首批提交 = **6 改动**

---

## 二、B5 Phase 0 · 测量基线 + 读现状 (5 min)

```bash
cd F:\zprintpro-nextjs
# 1. 查 IndexNow key (需 K3 提供, 8/24 提前)
cat .env.local | grep -i indexnow
# 或: ls .hermes\secrets\indexnow-key.json

# 2. sitemap URL 总数 (估算首批提交量)
curl -s https://zprintpro.com/sitemap.xml | grep -c "<loc>"

# 3. 3 闸门基线
npx tsc --noEmit
npm run build 2>&1 | tail -3
node scripts/check-encoding.js
```

**基线记录**: IndexNow key 有/无 / sitemap URL 总数 / tsc/build/encoding

---

## 三、B5 Phase 1 · 写脚本 + 集成 (30 min)

### Step 1.1 · scripts/indexnow-auto-submit.py (20 min, 自动化脚本)

```python
#!/usr/bin/env python3
"""
K3 v3.17 B5 IndexNow 自动化脚本
- 每日扫 sitemap.xml 解析全部 URL
- 与 .hermes/secrets/indexnow-history.json diff, 找新 URL
- POST IndexNow API (Bing + Yandex + Seznam + Naver)
- 提交结果落 .hermes/logs/indexnow-YYYY-MM-DD.json
- SOP-5 派生数据禁手搓: 脚本化生成 history + log
- 业务 0 改动: 增量提交, 不动 src/
"""

import json
import os
import urllib.request
import urllib.parse
from datetime import datetime
from pathlib import Path
import xml.etree.ElementTree as ET

# 配置
SITEMAP_URL = "https://zprintpro.com/sitemap.xml"
INDEXNOW_KEY_FILE = Path(__file__).parent.parent / ".hermes" / "secrets" / "indexnow-key.json"
INDEXNOW_HISTORY_FILE = Path(__file__).parent.parent / ".hermes" / "secrets" / "indexnow-history.json"
INDEXNOW_LOG_DIR = Path(__file__).parent.parent / ".hermes" / "logs"
KEY_LOCATION = "https://zprintpro.com/indexnow-key.txt"
INDEXNOW_API = "https://api.indexnow.org/indexnow"

def load_key():
    """读 IndexNow key (K3 拍板 8/24 提前注入, per G2 实体材料包)"""
    if not INDEXNOW_KEY_FILE.exists():
        raise FileNotFoundError(f"IndexNow key not found: {INDEXNOW_KEY_FILE}, K3 8/24 提前注入")
    return json.loads(INDEXNOW_KEY_FILE.read_text(encoding="utf-8"))["key"]

def load_history():
    """读历史提交 URL 列表"""
    if not INDEXNOW_HISTORY_FILE.exists():
        return []
    return json.loads(INDEXNOW_HISTORY_FILE.read_text(encoding="utf-8"))

def save_history(urls):
    """保存历史提交 URL 列表"""
    INDEXNOW_HISTORY_FILE.parent.mkdir(parents=True, exist_ok=True)
    INDEXNOW_HISTORY_FILE.write_text(
        json.dumps(urls, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )

def fetch_sitemap_urls():
    """fetch sitemap.xml 解析全部 URL"""
    req = urllib.request.Request(SITEMAP_URL, headers={"User-Agent": "K3-IndexNow-Bot/1.0"})
    with urllib.request.urlopen(req, timeout=30) as resp:
        xml_content = resp.read().decode("utf-8")
    # 解析 XML
    root = ET.fromstring(xml_content)
    ns = {"ns": "http://www.sitemaps.org/schemas/sitemap/0.9"}
    urls = []
    for url in root.findall(".//ns:loc", ns):
        urls.append(url.text.strip())
    return urls

def find_new_urls(all_urls, history):
    """找新 URL (未在 history 中)"""
    return [u for u in all_urls if u not in history]

def submit_indexnow(key, urls):
    """POST IndexNow API 批量提交 (最多 10000 URL/次)"""
    if not urls:
        return {"status": "no_new_urls"}
    # IndexNow 限制每次最多 10000 URL, 我们分批
    batches = [urls[i:i+10000] for i in range(0, len(urls), 10000)]
    results = []
    for batch in batches:
        payload = {
            "host": "zprintpro.com",
            "key": key,
            "keyLocation": KEY_LOCATION,
            "urlList": batch
        }
        data = json.dumps(payload).encode("utf-8")
        req = urllib.request.Request(
            INDEXNOW_API,
            data=data,
            headers={"Content-Type": "application/json"}
        )
        with urllib.request.urlopen(req, timeout=30) as resp:
            results.append({
                "status_code": resp.status,
                "submitted": len(batch)
            })
    return {"status": "ok", "results": results, "total_submitted": sum(r["submitted"] for r in results)}

def main():
    """主函数: 扫 sitemap + diff + 提交 + log"""
    print(f"[{datetime.now().isoformat()}] K3 v3.17 B5 IndexNow 自动提交启动")

    # 1. 读 key
    key = load_key()
    print(f"  IndexNow key loaded: {key[:8]}...")

    # 2. 扫 sitemap
    all_urls = fetch_sitemap_urls()
    print(f"  sitemap 解析: {len(all_urls)} URLs")

    # 3. diff 历史
    history = load_history()
    new_urls = find_new_urls(all_urls, history)
    print(f"  新 URL: {len(new_urls)} 个 (历史: {len(history)} 个)")

    # 4. 提交
    if not new_urls:
        result = {"status": "no_new_urls"}
    else:
        result = submit_indexnow(key, new_urls)
        # 5. 保存历史
        save_history(history + new_urls)

    # 6. log
    INDEXNOW_LOG_DIR.mkdir(parents=True, exist_ok=True)
    log_file = INDEXNOW_LOG_DIR / f"indexnow-{datetime.now().strftime('%Y-%m-%d')}.json"
    log_file.write_text(
        json.dumps({
            "timestamp": datetime.now().isoformat(),
            "all_urls_count": len(all_urls),
            "new_urls_count": len(new_urls),
            "new_urls_sample": new_urls[:10],
            "result": result
        }, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )
    print(f"  log 落盘: {log_file}")
    print(f"  result: {result}")
    print(f"[{datetime.now().isoformat()}] 完成")

if __name__ == "__main__":
    main()
```

### Step 1.2 · README 文档 (5 min, scripts/README-indexnow.md)

- 脚本使用说明
- K3 key 注入位置 (`.hermes/secrets/indexnow-key.json`)
- 监控: `python scripts/indexnow-auto-submit.py` 每日 1 次
- 退路: 手动 curl IndexNow API (B4 已用)

### Step 1.3 · .hermes/secrets/indexnow-key.json 模板 (2 min)

如果 K3 8/24 提供 key, 写实际值; 否则 8/25 K3 拍板后补

```json
{
  "key": "<K3_PROVIDED_INDEXNOW_KEY>",
  "created_at": "2026-08-24T17:30:00+08:00",
  "source": "K3 v3.17 B5"
}
```

### Step 1.4 · 4 cron prompt T39 段 (3 min, 集成 SSoT 模式)

通过 v3 SSoT §A 17 自动生效, 4 cron 启动必读 SSoT 第 1 优先级, 不需 mavis cron update (SOP-5 派生数据禁手搓 + SSoT 模式)

### Step 1.5 · 首批 B1-B4 新 URL 提交 (manual 1 次, push 后)

```bash
# K3 8/24 提前拍板 IndexNow key 后, 1 脚本跑批量提交
python scripts/indexnow-auto-submit.py
# 期望: 提交 657-660 URLs (B1-B4 新增约 7-10 个 + 既有 657 个)
```

---

## 四、B5 Phase 2 · 5 步 verify + commit + push + 4 cron update (15 min)

```bash
# 6 步验收 (per v3.17 §三)
node scripts/check-encoding.js       # 1. UTF-8 LF no-BOM
git status --short -- scripts/ .hermes\secrets\  # 2. untracked = 0 (脚本 + key + README)
npx tsc --noEmit                   # 3. 0 production error (TypeScript 不含 Python 脚本, 但需验证)
npm run build 2>&1 | tail -3       # 4. Compiled successfully + 39 routes
# 5. verify-deploy (push 后跑)
# 6. curl 1 連: 验证 IndexNow key 端点 (如果部署)
curl -sI "https://zprintpro.com/indexnow-key.txt" | head -1

# Python 脚本测试 (K3 key 注入后)
python scripts/indexnow-auto-submit.py
# 期望: log 落盘 + IndexNow 200 + history 保存

# 1 src commit 1 push 合并
git add scripts/indexnow-auto-submit.py
git add scripts/README-indexnow.md
git add .hermes/secrets/indexnow-key.json  # K3 key 注入后
git commit -m "feat(cron): T39 IndexNow 自动化 - 脚本化生成 sitemap 扫 + diff + 批量提交 + 4 cron 集成 (K3 v3.17 战略 8/24 11:37 B5 工具层殿后, 承接 B1-B4 全部新 URL 自动化 24h 索引)"
git push origin_ssh main
node scripts/verify-deploy.mjs

# 4 cron prompt T39 段通过 SSoT 模式自动生效 (4 cron 启动必读 SSoT §A 17 B5)
# 不需 mavis cron update (避免 buffer + 撞车)
```

---

## 五、退路 (唯一)

```bash
git revert HEAD --no-edit
git push origin_ssh main
node scripts/verify-deploy.mjs

# Python 脚本退路: 手动 curl IndexNow API
curl -X POST "https://api.indexnow.org/indexnow" \
  -H "Content-Type: application/json" \
  -d '{"host":"zprintpro.com","key":"<K3_KEY>","keyLocation":"https://zprintpro.com/indexnow-key.txt","urlList":[...]}'
```

**不允许**: 第二次试错 / 升级 CF Pages paid plan / 删任何 SKU/文案/长文本字段

---

## 六、报告 (落盘 `.hermes/logs/2026-08-24-B5-T39.md`)

K3 §0.21 6 行格式:
```
① commit sha: <commit>
② verify-deploy: ✅ run XXXXX success (或 ❌)
③ 3 闸门: tsc 0 production / build 39 routes / encoding 0 / Python 脚本 import 测试 PASS
④ curl 1 連: https://zprintpro.com/indexnow-key.txt 200 (K3 key 部署)
⑤ IndexNow 验证: 首次提交 7-10 新 URL + 657 既有 URL (B1-B4) 全部 200, log 落盘 .hermes/logs/indexnow-2026-08-24.json, history 保存 .hermes/secrets/indexnow-history.json
⑥ 退路触发: 否 (3 闸门全过 + IndexNow 200 + 4 cron 集成 SSoT 自动生效)
```

---

## 七、硬约束 (K3 v3.17 §四 + v3 增补 §A 2)

- ✅ 业务 0 改动红线: 不删 SKU/文案/长文本字段 (脚本化生成 history + log, 业务影响 0)
- ✅ SOP-5 派生数据禁手搓: 脚本化生成 history + log, 禁手写
- ✅ §13.16.1 ja ブランドワード「ジープリント」埋点 (脚本 metadata)
- ✅ 9 SOP 完整谱系
- ✅ 撞车兜底: 派活前 3 问 + 抢跑识别 tsc+build 必过

---

## 八、排期 (K3 v3.17 B5 17:30-18:15)

| 时间 | 任务 | 风险 |
|------|------|------|
| 17:30-17:35 | Phase 0 测量基线 (5 min) | 低 |
| 17:35-17:55 | Phase 1.1 IndexNow 自动化脚本 (20 min) | 中 (Python + urllib + XML + JSON) |
| 17:55-18:00 | Phase 1.2 README + 1.3 key 模板 (5 min) | 低 |
| 18:00-18:05 | Phase 1.4 cron 集成 (SSoT 模式) + 1.5 首批 URL 提交 (5 min) | 中 (K3 key 必含) |
| 18:05-18:10 | Phase 2 6 步 verify + commit + push (5 min) | 中 (1 src commit 1 push) |
| 18:10-18:15 | verify-deploy + 报告 + 升级 K3 (5 min) | 低 |

---

## 九、关联资源

- 战略: `docs/k3-strategy-v3.17-seven-tasks-today-2026-08-24.md` §一 B5
- v3.16: `docs/k3-strategy-v3.16-full-category-seo-geo-2026-08-22.md` §七 T39
- v3 增补: `.hermes/cron-prompts/k3-v3-addendum-2026-08-23.md` §A 17 v3.17 战略同步 (B5 17:30-18:15)
- B1-B4 上线: T45 + T42 + T44 + T41 + G1 Vol.2 src/ 改动已上线 (16:30 完成)
- IndexNow 文档: https://www.indexnow.org/

---

*签发: Mavis / 2026-08-24 12:05 / K3 v3.17 战略 8/24 11:37 B5 排期 17:30-18:15 / IndexNow 自动化脚本 + 4 cron 集成 (SSoT 模式) + 首批 B1-B4 新 URL 提交, 1 src commit 1 push*
*性质: B5 串行 P0, 工具层殿后, 承接 B1-B4 全部新 URL 自动化 24h 索引 (vs Google 7-14 天), 业务 0 改动红线不触发, §0.21 攒批作废立即 push, K3 key 必含 (008 度量层 8/29 校准已提前到 8/24)*
