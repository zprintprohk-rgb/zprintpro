fix(products): v7 修正 2 处 build error (line 18020 M3 v4 注释前缀恢复 + 删 boxStyle 字段)

【触发】K3 7/23 04:49 拍板 v7 任务卡 · 第一次 push (07a3ab6) CF Pages build FAIL · 2 处错误:
1. line 18020 注释前缀 `// ===== 新增 packaging SKU (M3 v4` 被 Python 脚本误删, 注释以 `· 2026-07-21)...` 开头 (TS 报 Unexpected character '·')
2. specs.boxStyle 字段不在 interface 定义 (TS 报 'boxStyle' does not exist)

【fix 1: 恢复 line 18020 M3 v4 注释前缀】

- src/data/products.ts line 18019-18020:
  - 旧: `  },\n  · 2026-07-21) — 卡盒/坑盒/插口盒 =====`
  - 新: `  },\n\n  // ===== 新增 packaging SKU (M3 v4 · 2026-07-21) — 卡盒/坑盒/插口盒 =====`
  - 根因: Python 脚本 end_pattern `r'\n  \},\n\n  // ===== 新增 packaging SKU \(M3 v4'` 匹配到 `\n  \},\n\n  ` 之后内容 = `  // ===== 新增 packaging SKU (M3 v4` 被保留 `\n  },\n\n  ` 之后所有内容, 但 M3 v4 注释**整行**保留 = `// ===== 新增 packaging SKU (M3 v4 · 2026-07-21)...` —— Python 替换后丢了 `// ===== 新增 packaging SKU (M3 v4` 之前所有内容 (因为在 pattern 匹配范围内), 保留的是 `· 2026-07-21) — 卡盒/坑盒/插口盒 =====` 这个 orphan 字符串

【fix 2: 删 specs.boxStyle 字段 (不在 interface 定义)】

- src/data/products.ts gang-run-card-boxes object specs section:
  - 旧: boxStyle 单独字段 ('飛機盒 / 普通扣底盒 / 雙插盒')
  - 新: 合并到 size 字段 ('8 檔標準尺寸, 飛機盒/扣底盒/雙插盒 3 種盒型')
  - 根因: specs interface (line 49-54) 只定义 material/size/printMethod/finishing 4 字段, boxStyle 不在 interface; TS 报 "'boxStyle' does not exist in type"
  - 修法选: 扩展 interface 加 boxStyle 字段 影响其他 SKU, 不推荐; 删 boxStyle 字段 合并到 size 字段 简单, 推荐

【CF Pages build 验证 (本地)】

- npm run build: ✓ Compiled successfully ✓ Generating static pages (3/3)
- 2 个 warning 跟 v7 无关 (sitemap-image.xml/robots.txt edge runtime + force-static 不兼容, 项目现状)
- 本地 build 通过, 1 push 触发 CF Pages 1 build

【commit 攒批】

- 本 commit 1 (fix) + 之前 v7 commit 1 (07a3ab6) = 2 commit
- push 1 次, CF Pages build 1 次 (跟 C9 攒批 1 push 1 build 节奏一致, 浪费 1 quota 是 v7 第一次 commit build 失败)
- 浪费 1 quota 的根因: Python 脚本 end_pattern 不严谨 + 缺 interface check; 教训: 大段 products.ts 改 + Python 脚本后必须本地 build 验证再 commit, 不能依赖 pre-commit check (pre-commit 只查 UTF-16/CRLF + 简体字, 不查 TS type)

【教训 (跨项目, 加 MEMORY.md)】

- Python 脚本正则替换大段 TS 代码后, 必须本地 `npm run build` 验证 (pre-commit check 不够)
- 替换后端 pattern 必须包含下个有效结构的完整前缀 (e.g. `// ===== 新增 packaging SKU (M3 v4` 整段保留)
- TS interface 字段检查: 加新字段前 grep 找 interface 定义, 不在就合并到已有字段 (避免扩 interface 影响其他对象)
- 修 products.ts 2 处错误 → 第二次 push 触发 build → build quota 浪费 1; 教训 = **第一次 commit 之前必须本地 npm run build 验证**
