# K3 四项拍板 · 战略大脑决策回执 (8/10 12:20)

> **K3 8/10 已拍板**: 第 1 项按 §0.15 实施 (zh-hk=智印港 / en=ZprintPro / ja=ジープリント)，en/ja 各自 brand 不变，**无需任何代码改动**。
> **本文**: 第 2/3/4 项由战略大脑决策（K3 授权"另外的决策一下"），M3 按此执行。

---

## 决策前代码事实核实 (2026-08-10 12:15 实测，非报告转述)

| 事实 | 状态 | 证据 |
|---|---|---|
| `seo.ts` getSiteName/locale 分支 | ✅ **已存在** (c48181b 品牌统一时落地) | seo.ts L24-26 zh-hk=智印港 ZprintPro / ja=ジープリント ZprintPro / en=ZprintPro |
| getSiteNAP 三语分支 | ✅ 已存在 | seo.ts L112-185 |
| 4 页面模板 siteConfig.name 直连 | ❌ **待改** (整合 push 第 1 项剩余) | blog/[slug] L817、press-kit L188、case-studies L357、about L253/263/269/275 |
| schema-extensions.ts `sku` 补全 | ❌ **未做** (GMC 项) | grep sku = 0 命中 |
| business-cards 禁区残留 | ⚠️ seo.ts L343-361 存在死数据 | 但 sitemap 0 条 business-cards URL = **未渲染，无线上风险** |

---

## 决策 2 · §0.20 教训固化 4 条 → **全部批准固化**

4 条候选全部有真实事故背书，批准入 AGENTS.md §0.20，**随 Batch A 合入**（见决策 3，今日 push 已满 3 次不再新增）：

1. **layout.tsx + seo.ts 静态 metadata 是 §0.15 升级盲区** — cefe895 第三次 push 就是补这个漏。规则：任何品牌/schema 改动必须同时检查 `layout.tsx` metadata + `generateMetadata` 静态段，grep `siteConfig.name` 全树清零才算完。
2. **3-part 完整链路** — retrofit 必须 3 件齐：blog-data JSON 正文 + blog-posts.ts meta + sitemap/验证 JSON，缺一即 PARTIAL 不报 PASS。
3. **GH013 止损** — commit 前必 `git status --porcelain` 看清 A/M/D；触发 secret 扫描立即 `git reset --mixed HEAD~1` 重做，**不 amend**；`.hermes/` 含 token 的历史报告永不入 commit。
4. **amend 月上限 1/2 已用满** (8/8 117f9fc + 8/10 8664488) — 本月后续任何需 amend 场景改走 revert + 重做路径。

## 决策 3 · 整合 push 拆分 → **Batch A 明日合入，Batch B 等 K3 三输入**

今日 push 已 3 次（超红线，K3 授权例外），**今日不再 push**。整合 push 拆两批：

**Batch A（零外部依赖，明日 8/11 与 paper-materials retrofit 合并 1 push）**：
1. 4 页面模板 `siteConfig.name` → `getSiteName(locale)`（helper 已存在，纯接线，~7 处）
2. `schema-extensions.ts` 补 `sku: product.slug`（GMC 强烈建议，PDP 实际 Product 段）
3. AGENTS.md §0.20 四条固化 + §0.15/0.16 段
4. 删除 seo.ts L343-361 business-cards 死数据（§11 强制，已确认无线上 URL，无需 301）
5. cron prompt v9.1（working tree 已 M）+ matrix.json v5 状态核对后随批
6. llms.txt / llms-zh-hk.txt / llms-ja.txt siteName locale 化

**Batch B（阻塞于 K3 三输入，输入齐后 30 分钟内可推）**：
- X URL + LinkedIn URL → Organization sameAs
- IndexNow key → ping 配置
- （审批文件 `integrated-push-approval.md` 仍 PENDING，K3 填 3 项改 STATUS=1-5 OK 即触发）

**依据**：Batch A 全部是 GEO 实体一致性闭环的必要件且零外部依赖，没有理由陪 Batch B 一起等；拆开让 P0 的实体闭环明天就能上线。

## 决策 4 · T10 任务卡缺失 → **落指针卡止噪，执行日 9/10 不变**

T10 裁决全文已在 `m3-task-cards/2026-08-09-qwen38-phase-strategy-8-10.md` §T10（新建 8 SKU + 301 收编旧 12，9/10 上线执行）。**不需要重复写一份完整卡**。已落指针卡 `m3-task-cards/2026-08-10-t10-seasonal-sku-pointer.md`，M3 cron 扫到即止，**不再每日上报"T10 缺失"**。8/29 前零代码动作（设计期 8/13-8/28）。

---

## M3 明日 (8/11) 执行清单

1. retrofit paper-materials（主线）
2. Batch A 六项合入同一 push（含 §0.20 固化 + business-cards 死数据清理）
3. push 前 §0.7 smoke 4 步 + push 后 verify-deploy
4. 台账：8/11 = 1 push（retrofit + Batch A 攒批）

**EOF · .hermes/k3-inbox/2026-08-10-1220-k3-4items-decision.md**
