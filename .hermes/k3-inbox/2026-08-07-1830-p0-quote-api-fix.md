# P0 紧急: /api/quote 写错 Supabase 表 — 修复报告 (K3 8/7 18:30 拍板 A)

**commit**: 改动落 working tree (未 push, K3 护栏 M3 不自主 push)
**diff stat**: 1 file changed, 35 insertions(+), 24 deletions(-) — 净增 11 行
**TS 验证**: 0 error in src/app/api/quote/route.ts (其他 19 error 历史遗留, 跟这次无关)

## 根因 (per 8/7 18:30 端到端实测)

| 字段 | 错误 (旧代码) | 正确 (新代码) |
|---|---|---|
| 表名 | `quote_calculations` (migration 003, 生产不存在) | `quotes` (migration 001, 已部署) |
| customer_name | optional (Zod) | **required (Zod + NOT NULL 表约束)** |
| customer_email | optional (Zod) | **required (Zod + NOT NULL 表约束)** |
| 字段映射 | product_slug / size_w / size_h / size_unit (3 列) | product_id (slug) / size (concat string) / turnaround / finishing |
| 缺失元数据 | 无 | ip_address / user_agent / referrer 全部入库 |

**业务影响**: 之前所有 /api/quote 提交全部 500 黑洞。`/api/order-notify` 200 OK 但只转发 formsubmit.co 邮件 (K3 收件箱), Mavis 看不到 → §6.1 询盘=0 验收不可能过。

## 修法 (per K3 8/7 18:30 拍板 A + 护栏)

### 改动 1: Zod schema 加 customer_name/email required

```ts
// 旧 (permissive):
customerName: z.string().optional(),
customerEmail: z.string().email().optional(),

// 新 (strict):
customerName: z.string().min(1, 'Customer name required'),
customerEmail: z.string().email('Valid email required'),
```

### 改动 2: 写 quotes 表 + 字段映射

```ts
// 旧: 写 quote_calculations (不存在, 500 黑洞)
const response = await fetch(`${supabaseUrl}/rest/v1/quote_calculations`, {
  ...
  body: JSON.stringify({
    product_slug: data.productSlug,
    quantity: data.quantity,
    size_w: data.size.w, size_h: data.size.h, size_unit: data.size.unit,
    material: data.material, finishes: data.finishes,
    deadline: data.deadline,
    unit_price: data.unitPrice, total_price: data.totalPrice,
    customer_name: data.customerName,  // optional
    customer_email: data.customerEmail, // optional
    ...
  })
});

// 新: 写 quotes (production-ready)
const sizeString = `${data.size.w}${...}x${data.size.h}${...}`;
const finishingString = data.finishes.length > 0 ? data.finishes.join(', ') : null;
const designNotes = `Product: ${data.productName}\nPrice: ${data.currency} ${data.totalPrice}\n...`;

const response = await fetch(`${supabaseUrl}/rest/v1/quotes`, {
  ...
  body: JSON.stringify({
    // 必填 (NOT NULL)
    customer_name: data.customerName,
    customer_email: data.customerEmail,
    product_id: data.productSlug,
    product_name: data.productName,
    quantity: data.quantity,
    // 可选
    customer_phone: data.customerPhone || null,
    material: data.material,
    size: sizeString,
    finishing: finishingString,
    turnaround: data.deadline,
    design_notes: designNotes,
    // 元数据
    ip_address: req.headers.get('x-forwarded-for')?.split(',')[0]?.trim() || null,
    user_agent: req.headers.get('user-agent'),
    referrer: data.referrerUrl || data.source,
    status: 'pending',
  })
});
```

## 测试方案 (待 K3 拍板 push 后跑)

### 1. Local smoke (改完 + 部署前)
```bash
cd F:\zprintpro-nextjs
node -e "
fetch('http://localhost:3000/api/quote/', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    productSlug: 'apparel-shopping-bag',
    productName: 'Apparel Shopping Bag',
    quantity: 100,
    size: {w: 300, h: 400, unit: 'mm'},
    material: 'kraft',
    finishes: ['matt-lamination'],
    deadline: 'standard',
    unitPrice: 12.5,
    totalPrice: 1250,
    currency: 'HKD',
    customerName: 'M3 8/7 verify',
    customerEmail: 'm3-verify-2026-08-07@zprintpro.com',
    customerPhone: '+8619880851334',
    customerCountry: 'HK',
    locale: 'zh-hk',
    source: 'M3-P0-verify'
  })
}).then(r => r.json()).then(console.log);
"
```

### 2. Production smoke (push 后跑)
```bash
$body = @{
  productSlug = 'apparel-shopping-bag'
  productName = 'Apparel Shopping Bag'
  quantity = 100
  size = @{w=300;h=400;unit='mm'}
  material = 'kraft'
  finishes = @('matt-lamination')
  deadline = 'standard'
  unitPrice = 12.5
  totalPrice = 1250
  currency = 'HKD'
  customerName = 'M3 8/7 prod verify'
  customerEmail = 'm3-verify-prod-2026-08-07@zprintpro.com'
  customerPhone = '+8619880851334'
  customerCountry = 'HK'
  locale = 'zh-hk'
  source = 'M3-P0-prod-verify'
} | ConvertTo-Json -Depth 5
Invoke-WebRequest -Uri 'https://zprintpro.com/api/quote/' -Method POST -Body $body -ContentType 'application/json'
```

### 3. Supabase 直接 verify (1A 配合)
```bash
# 用 SUPABASE_SERVICE_ROLE_KEY env 直接 SELECT count
node -e "
const { createClient } = require('@supabase/supabase-js');
const c = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
c.from('quotes').select('id, customer_email, created_at', { count: 'exact' }).order('created_at', { ascending: false }).limit(10).then(({data, count}) => console.log('Total quotes:', count, '\nLatest 10:', data));
"
```

期望:
- Local smoke → 200 + `id` UUID + `created_at` ISO timestamp
- Production smoke → 同上, 落 Supabase quotes 表
- Supabase 直接查 → 看到 M3 8/7 verify 那条记录

### 4. K3 收件箱 verify
- 查 zprintpro@outlook.com 邮箱 → 收到 /api/order-notify 转发邮件 (含 order details)
- 注: 这个不依赖 /api/quote, 是 formsubmit.co 主道

## 预期影响

| 指标 | 8/7 18:30 (修前) | 8/8 修后 (预期) |
|---|---|---|
| /api/quote HTTP | 500 黑洞 | 200 + 落 Supabase quotes 表 |
| 询盘数据可查 | 0 (K3 收件箱邮件外, 无任何查询入口) | ≥1 (Mavis 实时可查 Supabase) |
| §6.1 询盘≥5 验收 | 不可达 (黑洞) | 可达 (8/8-8/11 4 天可积累 5+ 真实询盘) |
| 客户感知 | 表单提交后 500 → UX 失败 | 表单提交 200 + 邮件 → 完整链路 |

## Push 策略 (待 K3 拍板)

K3 8/7 18:30 拍板 A + 护栏 "不走 E 全自主" → M3 已落 working tree, **不自主 push**.

3 个 push 时机选项 (待 K3 拍):
- **A. 立即独立 push** (破 §0.1 8/7 第 4 例外, K3 需明文授权)
- **B. 8/8 amend 合并 1 effective push** (per 3A 策略: 8/8 报告 + retrofit + P0 fix 3-in-1 amend, 节省 2 quota)
- **C. 8/8 11:00 weekly-meta cron 攒批** (per §0.6 攒批纪律, 跟 weekly meta refresh 合并)

**Mavis 建议**: B (8/8 amend 合并 1 push) — 最合规, 不破 §0.1 4 例外, 8/8 1 push 干 3 件事 (P0 fix + 8/8 retrofit + 8/8 日报)。

## 下一步

1. **立即**: 等 K3 拍板 push 时机 (A/B/C)
2. **8/8 09:00 起** (per K3 拍板): 跑 P0 端到端实测 (3 设备 walk through, 4 项 P0 修法都验)
3. **8/8 11:00**: retrofit cross-border-ecommerce-shipping-box-guide (P2)
4. **8/8 14:00-18:00**: 8/8 日报 + amend push + D1+D2 Supabase REST API 验证 (1A)
5. **8/9**: 301 传递 5/5 PASS (P1)
6. **8/10**: AI 可见性 ≥1/4 (P1)
7. **8/11**: P2 v8 retrofit 4 篇 (cross-border + baby-label + cmyk + paper-materials)
8. **8/12 复盘日**: review-8-12-template + §9 路径推荐 A/B/C/D

## 月度 push 配额

**29/500 (5.8%)** — 8/7 已用 3 push. 8/8 amend 合并 1 push 后 30/500 (6.0%). 健康.

## memory append (待 P0 fix 部署 PASS 后)

写入 MEMORY.md §10 跨项目教训:
- ❌ Supabase API 写代码用 "似乎对的" 表名 (quote_calculations) 而不 verify 生产实际表 (quotes)
- ✅ 任何 Supabase API endpoint 实施前必查 supabase/migrations/ 找表名 + 查生产 schema (`\d table_name` via MCP)
- ✅ 询盘/订单/支付 关键 funnel endpoint 必在本地 + 生产各跑 1 次端到端测试, 不只 tsc 0 error
- ✅ form 提交成功 (HTTP 200) ≠ 数据落库 (DB 写成功), 必双向 verify

## 跨项目教训 (待 P0 fix 部署 PASS 后)

任何 zprintpro / 跨项目 Supabase 集成, 必走 4 步:
1. 查 supabase/migrations/ 找表名 + schema
2. 跑 MCP / sql 查生产实际表 (`\d public.<table_name>`)
3. 写代码映射字段 (必填 + 可选 + 索引)
4. local + production 各跑 1 次端到端 (POST → 查 DB 落库)
