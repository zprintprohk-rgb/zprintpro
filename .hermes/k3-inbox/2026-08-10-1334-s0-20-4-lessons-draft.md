# AGENTS.md §0.20 4 条教训固化草稿 (K3 8/10 13:34 批准 · 明日 Batch A 写入)

> **签发**: Mavis · 2026-08-10 13:34 Asia/Shanghai
> **来源**: K3 8/10 13:34 决策 2 "§0.20 教训固化 4 条 → 全部批准固化"
> **写入时机**: 明日 Batch A 合入 AGENTS.md (8/11 daily cron + paper-materials retrofit 合并 1 push)
> **位置**: AGENTS.md 末尾 (在 §0.19 暂停信号规则 后新增 §0.20)

---

## §0.20 4 条教训固化草稿 (待 K3 8/11 拍板写入)

### §0.20.1 layout.tsx + seo.ts 静态 metadata 是 §0.15 升级盲区

**教训**: 任何品牌/schema 改动必须同时检查 `layout.tsx` metadata + `generateMetadata` 静态段, grep `siteConfig.name` + `hardcoded 'ZprintPro'` 全树清零才算完。

**事故背书 (2026-08-10)**: cefe895 commit 第三次 push 就是补这个漏。
- c48181b 改了 49 files 514 处 智印雲 → 智印港 (文本层 PASS)
- c48181b 后 curl 5 关键页面验证发现 `src/app/[locale]/layout.tsx` L43 hardcoded `siteName: 'ZprintPro'` 导致 en/ja/zh-hk 3 locale 全 locale og:site_name = ZprintPro (结构层漏)
- cefe895 改造 layout.tsx 静态 metadata → `generateMetadata` 函数 + `getLocaleBrand()` helper 3 locale
- 055d87e 改 src/lib/seo.ts 9 处 hardcoded `'ZprintPro'` ternary + 加 `getBrandName()` helper (schema 层漏)

**应用范围**:
- 任何 zprintpro / aitoptools / togthr 多 locale 项目
- 任何"主品牌 + locale 分层 + NAP 法律名"并存架构
- 任何 cron auto / 手动 / 紧急 push 改 src/lib/seo.ts / src/app/[locale]/layout.tsx

**判断 SOP** (任何品牌/schema 改造 commit 实施前自查):
1. grep `siteConfig.name` 全 src/ 树
2. grep `hardcoded 'ZprintPro'` 全 src/ 树
3. grep `'智印港' \? siteConfig.displayName : 'ZprintPro'` ternary 模式
4. 检查 layout.tsx 是否用 `export const metadata` 静态 vs `generateMetadata` 函数
5. 5/5 全部清零才报 PASS

### §0.20.2 retrofit 必 3 件齐：blog-data JSON + blog-posts meta + sitemap/验证 JSON

**教训**: 任何 retrofit 必 3 件齐, 缺一即 PARTIAL 不报 PASS。

**事故背书 (2026-08-09)**: baby-product retrofit 0d46a4c 完整 3 件齐 → v8_ready 100% + 5 步转化验证 verified + CF 93221129040 success。
- 8/10 cmyk-guide retrofit 8664488 跟 0d46a4c 同样 3 件齐 → v8_ready 100% + 5 块元素全 True 3 locale + CF 93335414345 success。

**应用范围**:
- 任何 retrofit / 数据迁移 / schema 迁移
- 任何"内容层 + meta 层 + 验证层"三段式改造
- 任何 cron auto / 手动 / 紧急 push 改 src/data/blog-data/ + src/data/blog-posts.ts

**判断 SOP** (任何 retrofit commit 实施前自查):
1. 改造前 grep 4 源: blog-data JSON + blog-posts meta + sitemap + 验证 JSON
2. 改造中保证 3 件同时改 (同 commit 或关联 commit)
3. 改造后跑 5 步转化验证 (CTA href + quote form + GA4 + wa.me + 失败标记)
4. sitemap mtime 验证 (build 后 lastmod 更新)
5. 3 件全 PASS 才报 PASS, 缺一报 PARTIAL

### §0.20.3 GitHub Push Protection 止损路径 (commit 前必查 + 触发后立即 reset)

**教训**: commit 前必 `git status --porcelain` 看清 A/M/D; 触发 secret 扫描立即 `git reset --mixed HEAD~1` 重做, **不 amend**; `.hermes/` 含 token 的历史报告永不入 commit。

**事故背书 (2026-08-10)**: c04dbe9 commit 误含 208 files (200+ .hermes/ 临时文件), 其中 `.hermes/reports/m3-p0-token-verify-fail-2026-07-29.md` 包含完整 Cloudflare User API Token (`cfut_Kf6BSzGQ...`), 触发 GH013 push protection。
- 修法: `git reset --mixed HEAD~1` 撤销 + 重 add 4 files only + 重 commit 8664488 (4 files only) + 重 push success = 1 effective push
- 实际节省 1 build 配额 (vs 2 commit 2 build), 但走了 fresh commit 路径不耗 amend 月上限

**应用范围**:
- 任何 zprintpro / aitoptools / togthr CF Pages 项目
- 任何 commit 含 .hermes/ 临时文件 + 报告
- 任何 token / secret / API key 在 .hermes/ 历史报告

**判断 SOP** (任何 commit 实施前自查):
1. 跑 `git status --porcelain` 看 A/M/D 全状态
2. A 状态文件 > 50 个 → 立即怀疑临时文件未 unstage
3. 跑 `git diff --cached --stat` 看清 staged 内容
4. 触发 GH013 → 立即 `git reset --mixed HEAD~1` + 重 add 4 files + 重 commit
5. 永远不 amend 触发 GH013 的 commit (节省 1 build 但污染 git history)

### §0.20.4 amend 月上限 1/2 已用满 (8/8 + 8/10) - 后续走 revert + 重做

**教训**: amend 月上限 1/2 (8/8 117f9fc + 8/10 8664488), 后续 2 次必走 revert + 重做路径。

**事故背书 (2026-08-10)**:
- 8/8 117f9fc force-with-lease amend 替代 4703262 失败 commit (TS duplicate property 错误)
- 8/10 8664488 fresh commit (不用 amend) 替代 c04dbe9 失败 commit (GH013 触发)
- 8 月已用 1/2, 剩余 1 次

**应用范围**:
- 任何 zprintpro / aitoptools / togthr CF Pages 项目
- 任何 amend force-with-lease / rebase push
- 任何需快速修正的失败 commit 场景

**判断 SOP** (任何 amend 需求自查):
1. 问"本月已用 amend 几次？还剩几次？"
2. 月上限 2 次用满 → 走 revert + 重做
3. amend 节省 1 build 但 force-push 也算 1 push 配额
4. amend 不算 1 build 配额节省, 算 1 push 配额消耗
5. 真的紧急才用 amend, 普通失败走 fresh commit 重做

---

## §0.20 段插入位置 (AGENTS.md 末尾)

```markdown
### §0.20 4 条教训固化 (2026-08-10 K3 拍板, 跨项目 P0)

(4 条全文如上, 整合到 AGENTS.md §0.20 段)
```

---

## §0.20 与之前 §0.10-0.19 关系

| 段 | 主题 | K3 拍板日 |
|---|---|---|
| §0.10 | KPI 校准硬约束 (4-5d 窗口) | 8/8 04:35 |
| §0.11 | 资源分配原则 (P0 抓强) | 8/8 04:35 |
| §0.12 | 转化侧指标 (询盘+响应时长) | 8/8 04:35 |
| §0.13 | K3 战略拍板 4 字+①②③ 模式 | 8/8 04:35 |
| §0.14 | CF Pages 配额校准 | 8/8 05:00 |
| §0.15 | 品牌一致性 P0 (locale-aware siteName) | 8/8 07:12 |
| §0.16 | 残留清理节奏 (~170/天 × 3 天) | 8/8 07:12 |
| §0.17 | push 台账一口径 | 8/8 15:35 |
| §0.18 | 重定向上线 SOP (4 步) | 8/8 15:35 |
| §0.19 | 用户暂停信号规则 (立即 kill cron) | 8/9 17:56 |
| **§0.20** | **4 条教训固化 (layout.tsx+seo.ts 盲区 / 3 件齐 / GH013 止损 / amend 1/2 月上限)** | **8/10 13:34** |

---

## 写入实施 plan (8/11 Batch A 跟 paper-materials retrofit 合并 1 push)

1. paper-materials retrofit (主线 3 locale)
2. Batch A 6 项 (含 AGENTS.md §0.20 4 条)
3. §0.7 smoke 4 步 (encoding + 简体字 + tsc + build)
4. 1 commit + 1 push (8/11 = 1 push, 13/150 monthly)
5. CF build verify PASS

EOF · .hermes/k3-inbox/2026-08-10-1334-s0-20-4-lessons-draft.md
