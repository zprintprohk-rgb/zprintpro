# 2026-08-21 K3 v3.10 修复: 简化 _redirects < 100 行 (CF Pages 限制)
# 删 SECTION 7 quote?product=* 78 行 (移到 CF Dashboard Bulk Redirects)
# 删 SECTION 8B {locale}/* 8 行 (同上)
# 保留 SECTION 1-6 + 8A + 8C + 9 + 10 (< 100 行)

# Cloudflare Pages Redirects
# 目标：统一URL规范为 https://zprintpro.com/（非www + https + 尾部斜杠）
# 
# Cloudflare Pages _redirects 规则说明：
# - 精确路径匹配（如 /en 只匹配 /en，不匹配 /en/xxx）
# - 通配符 * 可匹配路径任意部分
# - :splat 获取 * 匹配的内容
# - 查询参数默认保留（301 会自动传递 qs）
# - 不支持 host-level 匹配
# - 不支持路径 negation 语法
#
# 需在 Cloudflare 仪表板手动配置：
#   1. SSL/TLS → Edge Certificates → Always Use HTTPS
#   2. Rules → Page Rules: www.zprintpro.com/* 
#      301 → https://zprintpro.com/$1
# ============================================================

# =====================
# /\* /zh-hk/:splat/ 301

SECTION 2: Locale 尾部斜杠（精确匹配）
# =====================
/en /en/ 301
/zh-hk /zh-hk/ 301
/ja /ja/ 301

# =====================
# SECTION 3: Quote 尾部斜杠（精确匹配）
# =====================
/en/quote /en/quote/ 301
/zh-hk/quote /zh-hk/quote/ 301
/ja/quote /ja/quote/ 301
/quote /quote/ 301

# =====================
# SECTION 5: 路径修正 & 别名（已移除 /ja/guide/* → /ja/blog/ 重定向，因 sitemap 改用 /guide/ 路径）
# =====================

# =====================
# SECTION 6: Services 别名和旧链接修正
# =====================
# 【新增】Services 根路径 → rush-printing-delivery 详情页
# 解决 /zh-hk/services/、/en/services/、/ja/services/ 应为详情页而非列表页
/zh-hk/services/ /zh-hk/services/rush-printing-delivery/ 301
/en/services/ /en/services/rush-printing-delivery/ 301
/ja/services/ /ja/services/rush-printing-delivery/ 301

# same-day-printing-delivery → rush-printing-delivery（正确的slug）
# 同时覆盖带斜杠和不带斜杠的版本（Cloudflare精确匹配需要两者）
/zh-hk/services/same-day-printing-delivery /zh-hk/services/rush-printing-delivery/ 301
/zh-hk/services/same-day-printing-delivery/ /zh-hk/services/rush-printing-delivery/ 301
/en/services/same-day-printing-delivery /en/services/rush-printing-delivery/ 301
/en/services/same-day-printing-delivery/ /en/services/rush-printing-delivery/ 301
/ja/services/same-day-printing-delivery /ja/services/rush-printing-delivery/ 301
/ja/services/same-day-printing-delivery/ /ja/services/rush-printing-delivery/ 301

# =====================
# SECTION 8: 日文/中文编码 URL 404 修复
# 产品页面价格标签中的日文/中文字符被搜索引擎错误索引为URL路径
# 修复策略：301重定向到对应的产品页面或首页
# =====================
/個から /ja/ 301
/個起 /ja/ 301
/枚から /ja/quote/?product=custom-stickers 301
/張起 /ja/ 301

# =====================
# SECTION 8C: Cloudflare 内部路径 404 修复
# /cdn-cgi/ 是 Cloudflare 内部路径，不应被外部访问或索引
# =====================
/cdn-cgi/* / 301
# SECTION 9: 其他 404 修复
# =====================
/zh-hk/blog/flyer-guide/ /zh-hk/guide/ 301

# =====================
# SECTION 10: 旧平台兼容
# =====================
/product-detail?id=:id /zh-hk/product/:id/ 301