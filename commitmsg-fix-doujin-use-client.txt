fix: DoujinSKU 加 'use client' 修 JA 首页 500

- 根因: DoujinSKU 是 server component 但用了 onClick={() => trackProductView(...)}
  trackProductView → trackEvent → isAnalyticsEnabled() 引用 typeof window
  Next.js 14 SSR 阶段预计算 onClick handler 触发 window 引用 → SSR 500 (digest: 2080721686)
- 二分定位路径:
  1. 禁用 DoujinSKU → 200 ✓ (确认是 DoujinSKU 内部)
  2. 极简化 (只留标题) → 200 ✓
  3. 加 items.map (无 Link) → 200 ✓
  4. 加 <Link> wrap → 200 ✓
  5. 加 onClick → 500 ❌ ← 最小触发点
- 修法: 加 'use client' 到 DoujinSKU, 让 onClick 是 client-only, SSR 不预计算
- Tsc 0 errors
- 恢复完整 DoujinSKU 渲染 (5 SKU + 图 + Link + onClick + WhatsApp CTA)

Co-Authored-By: MiniMax <noreply@MiniMax.dev>
