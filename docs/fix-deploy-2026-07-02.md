# zprintpro fix-deploy PASS — 2026-07-02

## TL;DR

`d5c24b7` 修复了 `944358e` 推的 buggy code（page.tsx `'undefined'` slug + en 段提前 close），CF Pages build 现在 **SUCCESS**（run 84678052322）。

## Timeline

| 时间 (UTC+8) | 事件 | 结果 |
|---|---|---|
| 09:43 | commit `944358e` feat(seo): restore 3 GSC-driven blog posts | push OK |
| 10:05 | verify-deploy 报告 `CF Pages: failure` (run 84675634571) | ❌ build fail |
| 10:10 | 调试发现 page.tsx 缺 `m.slug` → `'undefined'` + en 段提前 close | — |
| 10:20 | commit `d5c24b7` fix(page): close en 段 prematurely + slug 'undefined' bug | push OK |
| 10:23 | verify-deploy 报 `no Cloudflare Pages check_run yet` | ⚠️ webhook 没 trigger |
| 10:25 | cron tick → verify-deploy 报 `CF Pages: success` (run 84678052322) | ✅ **PASS** |

## 根因（944358e 失败根因）

1. **buildPost 模板用 `m.slug` 拿 undefined**：`legacy-posts-meta.json` key 是 `zhhk_flyer` 等 locale-prefixed 形式，但 `buildPost(key)` 用 `meta[key].slug` 拿 slug — meta 里没 `slug` 字段，输出就是 `'undefined': { ... }`。
2. **en 段 patch anchor 错位**：原 HEAD 在 en 段 close 后有 3 个 'undefined' 孤儿 posts，我的 patch anchor `mtr-advertising-specs...,\n  },\n  ja: {` 把 en 段 close + 3 个 buggy posts 一起匹配，输出把 3 个新 posts 加在 close 之外，再次成为孤儿。
3. **pre-commit hook 漏检 TS 语法**：AGENTS.md §12 hook 只检查 UTF-8 LF / UTF-16 / BOM / size ratio，不做 TS parse 校验。**`TS1128: Declaration or statement expected`** 在 commit 之后才暴露。

## 修复 (d5c24b7)

| 修改 | 详情 |
|---|---|
| Slug 字段 | `'undefined':` × 9 → `'flyer-printing-guide':` / `'food-packaging-printing-guide':` / `'paper-bag-printing-guide':` |
| 字符串引号 | description / category 中 `"..."` → `'...'` (zh-hk + en 段 6 处) |
| en 段结构 | 删除 line 386 提前的 `  },`，让 3 个新 posts 留在 en 段内 |
| ja 段结构 | 删除 line 418 多余的 `  },` (v1 fix 后残留) |
| TS parse diagnostics | 24 → 0 |

## 当前 on main

```
d5c24b7 fix(page): close en 段 prematurely + slug 'undefined' bug from previous commit
944358e feat(seo): restore 3 GSC-driven blog posts (flyer/food-pkg/paper-bag) with matrix-generated covers
ae1c2a7 fix(verify-deploy): use shIn(cwd) wrapper so git rev-parse finds repo from any cwd
```

## 后续 SEO 验证（orchestrator 下一步）

按 memory "Hermes 任务报告'写日志不上线' → 真修复必须 orchestrator 介入" 的 6 步 verify：
1. sitemap 含 3 个新 URL（zh-hk/en/ja × 3 slug = 9 URLs）
2. curl 9 个 URL 都 200
3. content 含主关键词（食品包裝印刷 / 宣傳單張印刷 / 紙袋印刷）
4. FAQ 渲染（4 个 Q&A per locale）
5. Schema JSON-LD 注入（Article + BreadcrumbList + FAQPage）
6. IndexNow ping 202

## 教训 / Memory 更新候选

- **pre-commit hook 加 TS parse 校验**：UTF-8 LF / UTF-16 / BOM 是必要不充分，TS syntax error 仍能 commit。`scripts/pre-commit-check.mjs` 可加 `tsc --noEmit` 步骤。
- **buildPost 类模板函数要严格测 slug 字段**：避免 `m.slug` undefined 静默生成 `'undefined':` 这种灾难性字段。
- **patch anchor 要包含最近的合法 close 结构**：避免 anchor 匹配到错误位置。

## Auto-generated by Mavis orchestrator
mvs_6bfd88d733cb47e1b7b618a5f16604e5
2026-07-02 10:25 +08:00
